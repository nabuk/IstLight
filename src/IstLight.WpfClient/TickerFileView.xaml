<UserControl x:Class="IstLight.TickerFileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:helpers="clr-namespace:IstLight.Helpers"
             mc:Ignorable="d" 
             d:DesignWidth="300"
             helpers:StateManager.VisualState="{Binding LoadState, Mode=OneWay}"
             >
    <Border
        x:Name="mainBorder"
        Background="{StaticResource NormalBrush}">
        <Grid Margin="2">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            
            <DockPanel>
                <Button DockPanel.Dock="Right"
                    Content="Remove"
                    Command="{Binding CloseCommand}"
                    VerticalAlignment="Top"
                    Padding="6,2"
                    />
                
                <TextBox
                    x:Name="tbIndex"
                    Text="{Binding Index}"
                    Foreground="Gray"
                    IsReadOnly="True"
                    VerticalAlignment="Center"
                    Width="25"
                    TextAlignment="Right"
                    />

                <TextBox
                    x:Name="tbSymbol"
                    Text="{Binding Name}"
                    Foreground="Gray"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Left"
                    Width="150"
                    Margin="3"
                    IsReadOnly="True"
                />
            </DockPanel>

            <ProgressBar
                x:Name="loadProgress"
                IsIndeterminate="True"
                Grid.Row="1"
                Height="4"
                VerticalAlignment="Bottom" />
        </Grid>
        
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="LoadStates">
                <VisualStateGroup.Transitions>
                    <VisualTransition GeneratedDuration="0:0:1"/>
                    <VisualTransition GeneratedDuration="0:0:0" To="Running"/>
                    <VisualTransition GeneratedDuration="0:0:0.4" To="Error"/>
                    <VisualTransition GeneratedDuration="0:0:0.4" To="Completed"/>
                </VisualStateGroup.Transitions>
                <!--<VisualState x:Name="Running">
                    <Storyboard>
                        <ColorAnimation Storyboard.TargetProperty="(TextElement.Foreground).(SolidColorBrush.Color)" Storyboard.TargetName="tbSymbol"
                                    To="{StaticResource BlackColor}" Duration="0:0:0" />
                    </Storyboard>
                </VisualState>-->
                <VisualState x:Name="Completed">
                    <Storyboard>
                        <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" Storyboard.TargetName="mainBorder"
                            To="#889B" Duration="0" />
                        <ColorAnimation Storyboard.TargetProperty="(TextElement.Foreground).(SolidColorBrush.Color)" Storyboard.TargetName="tbSymbol"
                                    To="{StaticResource WhiteColor}" Duration="0" />
                        <DoubleAnimation Storyboard.TargetProperty="Opacity" Storyboard.TargetName="loadProgress"
                            To="0" Duration="0" />
                        <BooleanAnimationUsingKeyFrames Storyboard.TargetProperty="IsReadOnly" Storyboard.TargetName="tbSymbol">
                            <DiscreteBooleanKeyFrame Value="False" />
                        </BooleanAnimationUsingKeyFrames>
                    </Storyboard>
                </VisualState>
                <VisualState x:Name="Error">
                    <Storyboard>
                        <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" Storyboard.TargetName="mainBorder"
                            To="#4F00" Duration="0" />
                        <DoubleAnimation Storyboard.TargetProperty="Opacity" Storyboard.TargetName="loadProgress"
                            To="0" Duration="0" />
                    </Storyboard>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Border>
</UserControl>
